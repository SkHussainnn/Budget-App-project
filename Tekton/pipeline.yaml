apiVersion: tekton.dev/v1beta1
kind: Pipeline
metadata:
  name: build-and-deploy-pipeline
  namespace: tekton-pipelines-secure
spec:
  params:
    - name: url
      type: string
      default: "https://github.com/SkHussainnn/Budget-App-project.git"
    - name: tag
      type: string
      default: "skhussain786/myrail-app:1.0"
  tasks:
    - name: build-and-push
      taskRef:
        name: build-and-push
      params:
        - name: url
          value: $(params.url)
        - name: tag
          value: $(params.tag)
